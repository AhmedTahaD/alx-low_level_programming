#include "main.h"
/**
 * mul - function to multiply two integers
 * @a:first input
 * @b:second input
 * Return: returns result
 */
int mul(int a, int b)
{
	int result;

	result = a * b;

	return (result);
}
